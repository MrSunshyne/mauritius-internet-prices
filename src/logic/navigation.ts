import { HomeIcon } from '@heroicons/vue/outline'
import { MenuItem } from '@/types'

const menu: MenuItem[] = [
  { path: '/', name: 'Home', icon: HomeIcon },
  { path: '/test', name: 'Test', icon: HomeIcon },
]

export default menu
